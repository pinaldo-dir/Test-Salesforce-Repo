<?xml version="1.0" encoding="utf-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Sort_Order_Non_Formula__c</fullName>
    <description>Created to serve as a non-formula sorting field in SOQL; becomes populated via workflow with the value in the Sort_Order__c field on the object.  That value is then used to sort in the output since you cannot do so with formulas.</description>
    <externalId>false</externalId>
    <label>Sort Order Non-Formula</label>
    <length>5</length>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
