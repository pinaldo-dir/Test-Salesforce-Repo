<?xml version="1.0" encoding="utf-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Phone__c</fullName>
    <externalId>false</externalId>
    <formula>if(NOT(ISBLANK(Contact__c)), Contact__r.Phone &amp; IF(NOT(ISBLANK(Contact__r.Phone_Ext__c)), ' x' &amp; Contact__r.Phone_Ext__c, ''), IF(NOT(ISBlANK(Account__c)), Account__r.Phone &amp; IF(NOT(ISBLANK(Account__r.Phone_Ext__c)), ' x' &amp; Account__r.Phone_Ext__c, ''), ''))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Phone</label>
    <required>false</required>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
